<div id="<%= dom_id board %>" class="w-full sm:w-auto my-5 space-y-5 flex gap-10">
  <div>
    
    <h1 class="text-2xl font-bold"><%= board.title %></h1>
  </div>
  <div>
    
    <p class="text-gray-600"><%= board.description %></p>
  </div>

</div>
